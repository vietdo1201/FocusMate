# Cross-device test index

Thư mục dành cho golden vectors và replay data synthetic của `FaceObservationV1`. Codec/unit test hiện nằm trong module `protocol`; chưa có BLE/firmware integration fixture.
